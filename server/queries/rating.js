const Rating = require('../models').Rating;

const ratingQuery = {};

// post a rating
// update a rating
// remove a rating
// calculate beacon rating average
// filter by rating

module.exports = ratingQuery;
