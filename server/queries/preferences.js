const Preferences = require('../models').Preferences;

const preferencesQuery = {};

// add preferences
// remove preferences
// update preferences
// find preferences by user

module.exports = preferencesQuery;
